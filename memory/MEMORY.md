<!--
  This is the memory INDEX. It is loaded into context every session — it is the
  agent's table of contents for everything it remembers. One line per memory:
  a link, then a short hook. Never put memory CONTENT here, only the pointer.
  The full fact lives in the linked file and is pulled in by relevance.
-->

# Memory index

- [Prefers draft PRs](prefers-draft-prs.example.md) — always open pull requests as drafts to keep CI quiet until reviewed
